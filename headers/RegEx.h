#pragma once

bool nameExp( QString& string );
bool emailExp( QString& string );
bool phoneExp( QString& string );
bool addressExp( QString& string );
QDate dateExp( QString& string ) noexcept(false);
